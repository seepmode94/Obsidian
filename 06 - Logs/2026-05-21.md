> **Anteriores:** [[2026-05-20]]

## Pendentes Anteriores
```dataviewjs
const cur = dv.current().file;
const links = cur.outlinks.values.filter(l => /^\d{4}-\d{2}-\d{2}$/.test(l.fileName()));
if (links.length === 0) {
  dv.paragraph("_Sem ligação a log anterior._");
} else {
  for (const link of links) {
    const page = dv.page(link.path);
    if (!page) { dv.paragraph(`[[${link.fileName()}]] _(ficheiro não existe)_`); continue; }
    const pending = page.file.tasks
      .where(t => !t.completed && t.text.trim().length > 0 && t.header.subpath === "Foco de Hoje");
    dv.paragraph(`De [[${page.file.name}]]:`);
    if (pending.length === 0) dv.paragraph("_Tudo concluído._");
    else dv.taskList(pending, false);
  }
}
```

## Foco de Hoje
- [ ] https://github.com/CRM-Seepmode/LoungeCRM/issues/20
- [ ] [https://github.com/OpsDock/cstaff-platform/issues/17](https://github.com/OpsDock/cstaff-platform/issues/17#issue-4277400303 "https://github.com/opsdock/cstaff-platform/issues/17#issue-4277400303")

## Para Não Me Perder
*De dias anteriores linkados acima (secção "Para Não Me Perder"):*

```dataviewjs
const cur = dv.current().file;
const links = cur.outlinks.values.filter(l => /^\d{4}-\d{2}-\d{2}$/.test(l.fileName()));
if (links.length === 0) {
  dv.paragraph("_Sem ligação a logs anteriores._");
} else {
  let total = 0;
  for (const link of links.sort((a,b) => b.fileName().localeCompare(a.fileName()))) {
    const page = dv.page(link.path);
    if (!page) continue;
    const pending = page.file.tasks
      .where(t => !t.completed && t.text.trim().length > 0 && t.header.subpath === "Para Não Me Perder");
    if (pending.length > 0) {
      dv.paragraph(`De [[${page.file.name}]]:`);
      dv.taskList(pending, false);
      total += pending.length;
    }
  }
  if (total === 0) dv.paragraph("_Nada em aberto nos linkados._");
}
```

**Novos hoje:**
- [ ] em waiting review CRM paridade com studio(clientes filtros) ✅ 2026-05-21

## Bloqueios
- 

## Próximo Passo
- 
